package com.codecool.emem.game.gear;

public class Armor extends Gear {

    public Armor(int attackBonus, int defenseBonus, int damageBonus, int armorBonus) {
        super(attackBonus, defenseBonus, damageBonus, armorBonus);
    }
}
